<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourname.habit360">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight">

        <!-- Splash Activity -->
        <activity
            android:name="com.vishwawijekoon.habit360.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name="com.vishwawijekoon.habit360.activities.LoginActivity"
            android:exported="false" />

        <!-- SignUp Activity -->
        <activity
            android:name="com.vishwawijekoon.habit360.activities.SignUpActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name="com.vishwawijekoon.habit360.activities.MainActivity"
            android:exported="false" />

        <!-- Hydration Receiver -->
        <receiver
            android:name="com.vishwawijekoon.habit360.notifications.HydrationReceiver"
            android:exported="false" />
    </application>

</manifest>
